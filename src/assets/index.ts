import logoDark from "./logo-dark.png";
import logoLight from "./logo-light.png";
import noImage from "./no-image.png";

// logoDark: the blue-on-white/transparent mark — use on LIGHT backgrounds.
// logoLight: the pale/outline mark — use on DARK backgrounds.
// noImage: fallback thumbnail for products that have no uploaded image yet.
export const brandAssets = {
  logoDark,
  logoLight,
  noImage,
};
